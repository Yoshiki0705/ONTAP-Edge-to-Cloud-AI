# Edge (Raspberry Pi)
paho-mqtt>=2.0.0
boto3>=1.34.0
gpiozero>=2.0.0
requests>=2.31.0
numpy>=1.26.0
confluent-kafka>=2.4.0

# Data processing
pandas>=2.2.0
pyarrow>=15.0.0

# AI/ML
langchain-aws>=0.2.0

# Development and CI tooling lives in requirements-dev.txt with exact pins.
# It used to be listed here as `pytest>=8.0.0` / `cfn-lint>=0.87.0` as well, so
# two files described the same tools and the ranges let PATH and .venv disagree
# (measured: cfn-lint 1.52.1 vs 1.52.0). One source now:
#   make dev-install
