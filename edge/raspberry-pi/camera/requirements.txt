# Edge camera capture dependencies — exact pins.
#
# OpenCV stays on 4.x and NumPy on 1.26.x together: the 4.x wheels are built
# against the NumPy 1.x ABI. Bumping either alone is what breaks the device.
opencv-python-headless==4.14.0.94
numpy==1.26.4
boto3==1.43.89
requests==2.34.2
