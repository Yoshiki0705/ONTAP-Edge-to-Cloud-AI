name: OpenSSF Scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 0 * * 1'  # Weekly on Monday

permissions: read-all

jobs:
  analysis:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      id-token: write
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - uses: ossf/scorecard-action@f49aabe0b5af0936a0987cfb85d86b75731b0186 # v2.4.1
        id: scorecard
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: true

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: always()
        with:
          name: SARIF file
          path: results.sarif
          retention-days: 5

        # The pin before last, ce28f5bb commented v3.28.0, is not a commit in that
        # repository at all, so every run failed at action resolution before a step
        # ran. scripts/check_action_pins.py --verify is what now asks GitHub whether
        # a pin exists and matches the version its comment claims.
        #
        # v4, not v3: the CodeQL Action v3 line is announced for deprecation in
        # December 2026, which its own run log warns about. The same commit is used
        # by codeql.yml so the tree names one version.
      - uses: github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        if: always()
        with:
          sarif_file: results.sarif
